; Increase limits for all uploads (products, templates, design assets, etc.).
; PHP-FPM reads .user.ini from the document root at runtime.
upload_max_filesize = 64M
post_max_size = 100M
memory_limit = 256M
max_execution_time = 120
max_input_time = 120
